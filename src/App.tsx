import ListGroup from "./components/ListGroup";

function App() {
  return <div><ListGroup /></div>;
  // The App component is a functional component in React that serves as the main entry point for the application.
  // It returns a JSX element that renders the ListGroup component inside a div.
  // The ListGroup component is imported from the components directory and is used to display a list of items.
}

export default App;
//The export default statement is used to export a single value or component from a module, making it the default export of that module.